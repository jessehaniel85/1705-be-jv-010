server:
  port: 8081

spring:
  application:
    name: comprovante-emissor
  jackson:
    property-naming-strategy: SNAKE_CASE   # JSON em snake_case (contrato do módulo)
    default-property-inclusion: non_null

# ------------------------------------------------------------------------------
# TODO (Aula 4/5) — configuração da fila por perfil:
#   plano-a-docker: spring.rabbitmq.host/port apontando para o RabbitMQ do Docker
#   plano-b-jvm:    broker Qpid embarcado (subir no boot) + spring.rabbitmq.* em localhost
#   plano-c:        sem broker — publicar via ApplicationEventPublisher
# ------------------------------------------------------------------------------

management:
  endpoints:
    web:
      exposure:
        include: health,info
